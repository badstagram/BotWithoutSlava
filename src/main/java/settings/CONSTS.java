package settings;

public class CONSTS {
    //не публиковать токен т.к. из-за этого он меняется(Don't publish Bot token because if you do this the token will change. *publishes the token*)
    public static final String token = "";
    public static final String prefix = "&";
}
